{
  "name": "opencreature",
  "version": "0.1.0",
  "description": "Animated terminal creatures (raccoon, troll, ogre, pigeon). Standalone toy.",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "opencreature": "dist/cli.js"
  },
  "files": ["dist", "README.md"],
  "keywords": ["cli", "ascii", "animation", "ink", "terminal"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/VanilaDreams/opencreature.git",
    "directory": "packages/cli"
  },
  "scripts": {
    "build": "bun build src/cli.tsx --target=node --outdir dist --format=esm --banner='#!/usr/bin/env node' --external react --external react-dom --external ink --external chalk --external react-devtools-core && chmod +x dist/cli.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "bun src/cli.tsx"
  },
  "dependencies": {
    "ink": "5.0.1",
    "react": "18.3.1",
    "chalk": "5.3.0"
  },
  "devDependencies": {
    "@types/react": "18.3.12"
  }
}
